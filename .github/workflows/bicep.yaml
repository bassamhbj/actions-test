name: Azure Bicep CD

on: [workflow_dispatch]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        path: repo

    - uses: azure/login@1
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        susbcription: ${{ secrets.AZURE_SUBSCRIPTION }}

    - run: echo "💡 ${{ vars.NAME }}"